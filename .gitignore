# Dependencies
node_modules/

# Build output
dist/
build/

# Environment files (NEVER commit these — they contain secrets)
.env
.env.local
.env.development
.env.production
.env.staging

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# Editor directories
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# NestJS compiled JS (we use ts-node in dev)
*.js.map

# Prisma (we keep migrations, but not the generated client binary)
# prisma/migrations is committed (it's our DB schema history)

# Test coverage
coverage/
